# to print all the elements within a list in the reverse order.To entire list has to be passed via command line arguments(Hint : use argv)
import sys
print("elements in argument list before reversing :",sys.argv)
print("after reversing",sys.argv[::-1])